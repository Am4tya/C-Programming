#include <stdio.h>
//  Dynamic memory allocation
// The process of allocating memory during program execution is called Dynamic Memory Allocation..


// use stdlib.h header file 
// malloc(), calloc(), reaLoc(), free()
int main(){
    int arr[10];
	printf("%d",sizeof(arr));

return 0;
}
